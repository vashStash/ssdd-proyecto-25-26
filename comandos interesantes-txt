docker compose -f docker-compose-devel-mongo.yml up --build

docker exec -it proyecto-db-mongo-1 -uroot -p

docker container rm proyecto-db-mongo-1


para hacer una reseteadita
docker system prune -a
docker cpmpose build -f el fichero docker que quieras

Para solucionar el problema en MAC copiar esto en el engine de docker:
{
  "insecure-registries": [
    "hub.docker.com",
    "registry-1.docker.io",
    "docker.io"
  ]
}
